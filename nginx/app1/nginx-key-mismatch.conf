events {}

http {
    server {
        listen 443 ssl;
        server_name app1.lab.local;

        # Deliberately pair App1's certificate with an unrelated private key.
        ssl_certificate     /etc/nginx/tls/app1.crt;
        ssl_certificate_key /etc/nginx/tls/wrong.key;
    }
}
